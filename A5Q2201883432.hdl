CHIP A5Q2201883432 {
    IN X, T, U;
    OUT O;

    PARTS:
    Not(in=X, out=Xnot);
    Not(in=T, out=Tnot);
    Not(in=U, out=Unot);

    And(a=Xnot, b=Tnot, out=and1);
    Or(a=and1, b=Unot, out=O);
}
